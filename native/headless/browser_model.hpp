#pragma once

#include "host/browser_model.hpp"

namespace strata::headless {
using BrowserBounds = host::BrowserBounds;
using BrowserElement = host::BrowserElement;
using BrowserModel = host::BrowserModel;
} // namespace strata::headless
